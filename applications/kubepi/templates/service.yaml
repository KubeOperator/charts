apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubepi.fullname" . }}
  labels:
    {{- include "kubepi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubepi.selectorLabels" . | nindent 4 }}
